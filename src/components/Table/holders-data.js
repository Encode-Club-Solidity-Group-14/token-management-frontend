export function createData(address, quantity, percentage, usdValue) {
  return { address, quantity, percentage, usdValue };
}

export const holdersHeaders = [
  "Address",
  "Quantity",
  "Percentage",
  "USD Value",
];
